# PortOne V2 API key. PortOne 콘솔의 "내 결제 - API Keys" 에서 발급.
# 본 서버는 PortOne 결제 조회 외의 쓰기 권한이 필요 없으니 read-only 가능
# 하면 read-only 키 사용 권장.
PORTONE_API_KEY=

# 대시보드 로그인 비밀번호. 비워두면 누구나 접근 가능 → 반드시 설정.
DASHBOARD_PASSWORD=

# 세션 쿠키 서명용 시크릿. openssl rand -hex 32 같은 결과 추천.
SESSION_SECRET=
